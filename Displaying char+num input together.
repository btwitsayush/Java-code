/*
Given a string S and an integer number N,
print the output in the form: "The string is :S" and "The interger is :N"
in the two separate lines.

Input Format

The first line accept a string
The Second line accept a Number

For Example

Input:
S = 'LPU'
N = 20
Output :
The string is :LPU
The integer is :20

Constraints

N is positive integer

Output Format

The string is :LPU
The integer is :20

Sample Input 0

Rama
50
Sample Output 0

The string is :Rama
The integer is :50
*/

ans-------------
import java.io.*;
import java.util.*;

public class Solution {

    public static void main(String[] args) {
        Scanner s=new Scanner(System.in);
        String str =s.next();//Taling char input from user
        int num =s.nextInt();//Taking num input from user
        System.out.println("The string is :" + str);
         System.out.println("The integer is :" + num);
    }
}
